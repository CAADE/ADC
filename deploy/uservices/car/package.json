{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-car",
    "publish": "docker push ${CAADE_REGISTRY}/adc-car",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-car"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
