{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build .",
    "publish": "docker push localhost:5000/adc/car",
    "deploy": "docker push localhost:5000/adc/car"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
