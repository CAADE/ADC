{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-ai",
    "publish": "docker push ${CAADE_REGISTRY}/adc-ai",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-ai"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
