{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t localhost:5000/adc/streaming",
    "publish": "docker push localhost:5000/adc/streaming",
    "deploy": "docker push localhost:5000/adc/streaming"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
