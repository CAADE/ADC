{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-streaming",
    "publish": "docker push ${CAADE_REGISTRY}/adc-streaming",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-streaming"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
