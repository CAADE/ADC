{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-alert-system",
    "publish": "docker push ${CAADE_REGISTRY}/adc-alert-system",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-alert-system"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
