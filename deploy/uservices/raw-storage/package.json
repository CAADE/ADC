{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-raw-storage",
    "publish": "docker push ${CAADE_REGISTRY}/adc-raw-storage",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-raw-storage"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
