{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-annotation",
    "publish": "docker push ${CAADE_REGISTRY}/adc-annotation",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-annotation"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
