{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build .",
    "publish": "docker push localhost:5000/adc/notification-gateway",
    "deploy": "docker push localhost:5000/adc/notification-gateway"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
