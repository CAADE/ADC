{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/adc-notification-gateway",
    "publish": "docker push ${CAADE_REGISTRY}/adc-notification-gateway",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-notification-gateway"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
