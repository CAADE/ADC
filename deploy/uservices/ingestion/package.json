{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t localhots:5000/adc/ingestion",
    "publish": "docker push ${CAADE_REGISTRY}/adc-ingestion",
    "deploy": "docker push ${CAADE_REGISTRY}/adc-ingestion"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
