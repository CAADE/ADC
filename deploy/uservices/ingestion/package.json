{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build .",
    "publish": "docker push localhost:5000/adc/ingestion",
    "deploy": "docker push localhost:5000/adc/ingestion"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
