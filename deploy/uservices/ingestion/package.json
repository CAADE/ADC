{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t madajaju/adc-ingestion",
    "publish": "docker push madajaju/adc-ingestion",
    "deploy": "docker push madajaju/adc-ingestion"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
