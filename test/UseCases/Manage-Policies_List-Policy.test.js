var Promise = require('bluebird');

describe('Use Case Manage-Policies', function () {
  describe('List-Policy Scenario', function () {
    it('List-Policy Primary Test', function (done) {
       done();
    });
  });
});
