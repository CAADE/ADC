var Promise = require('bluebird');

describe('Use Case Register-Car', function () {
  describe('Register-Car Scenario', function () {
    it('Register-Car Primary Test', function (done) {
       done();
    });
  });
});
