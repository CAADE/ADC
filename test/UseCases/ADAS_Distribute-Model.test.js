var Promise = require('bluebird');

describe('Use Case ADAS', () => {
  describe('Distribute-Model Scenario', () => {
    it('Distribute-Model Primary Test', (done) => {
       done();
    });
  });
});
