var Promise = require('bluebird');

describe('Use Case ADAS', function () {
  describe('Train-Models Scenario', function () {
    it('Train-Models Primary Test', function (done) {
       done();
    });
  });
});
