var Promise = require('bluebird');

describe('Use Case Manage-Cloudlets', function () {
  describe('Add-Cloudlet Scenario', function () {
    it('Add-Cloudlet Primary Test', function (done) {
       done();
    });
  });
});
