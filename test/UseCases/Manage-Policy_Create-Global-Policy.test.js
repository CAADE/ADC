var Promise = require('bluebird');

describe('Use Case Manage-Policy', () => {
  describe('Create-Global-Policy Scenario', () => {
    it('Create-Global-Policy Primary Test', (done) => {
       done();
    });
  });
});
