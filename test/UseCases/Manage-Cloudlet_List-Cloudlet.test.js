var Promise = require('bluebird');

describe('Use Case Manage-Cloudlet', () => {
  describe('List-Cloudlet Scenario', () => {
    it('List-Cloudlet Primary Test', (done) => {
       done();
    });
  });
});
