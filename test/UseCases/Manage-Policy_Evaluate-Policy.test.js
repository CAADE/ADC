var Promise = require('bluebird');

describe('Use Case Manage-Policy', () => {
  describe('Evaluate-Policy Scenario', () => {
    it('Evaluate-Policy Primary Test', (done) => {
       done();
    });
  });
});
