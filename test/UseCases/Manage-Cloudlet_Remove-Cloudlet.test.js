var Promise = require('bluebird');

describe('Use Case Manage-Cloudlet', () => {
  describe('Remove-Cloudlet Scenario', () => {
    it('Remove-Cloudlet Primary Test', (done) => {
       done();
    });
  });
});
