var Promise = require('bluebird');

describe('Use Case Deregister-Car', function () {
  describe('Deregister-Car Scenario', function () {
    it('Deregister-Car Primary Test', function (done) {
       done();
    });
  });
});
