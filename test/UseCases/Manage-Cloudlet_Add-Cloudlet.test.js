var Promise = require('bluebird');

describe('Use Case Manage-Cloudlet', () => {
  describe('Add-Cloudlet Scenario', () => {
    it('Add-Cloudlet Primary Test', (done) => {
       done();
    });
  });
});
