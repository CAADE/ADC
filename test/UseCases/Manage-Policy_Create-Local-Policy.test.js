var Promise = require('bluebird');

describe('Use Case Manage-Policy', () => {
  describe('Create-Local-Policy Scenario', () => {
    it('Create-Local-Policy Primary Test', (done) => {
       done();
    });
  });
});
