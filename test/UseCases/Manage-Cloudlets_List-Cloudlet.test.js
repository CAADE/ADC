var Promise = require('bluebird');

describe('Use Case Manage-Cloudlets', function () {
  describe('List-Cloudlet Scenario', function () {
    it('List-Cloudlet Primary Test', function (done) {
       done();
    });
  });
});
